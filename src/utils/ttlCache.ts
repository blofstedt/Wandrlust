/**
 * Re-exported from shared/ttlCache.ts, which both this client bundle and the
 * server import — see that file for why it lives there instead of here.
 */
export { TtlCache } from '../../shared/ttlCache';
